# AP-013C — Real PC/EAGLE/OneDrive OAT Evidence — 2026-09-04

| Field | Value |
|---|---|
| Evidence ID | `E-AP013C-OAT-2026-09-04` |
| Package | AP-013C — Verified Transport Cleanup and Convergence Monitoring |
| Backlog | BKL-047 |
| Date | 04/09/2026 |
| Mode | `DRY_RUN / NO_DELETE` |
| PC | `WIN-QOOF3903TQS` |
| EAGLE | `EAGLE30154` |
| Branch baseline | `architecture/ap-013c-verified-transport-cleanup` |
| Runtime code baseline | `78b104c5732f353ca88ac0f3eced1de2ed1af14b` |
| Productive cleanup | **Not authorized** |

## 1. Purpose

Preserve the controlled real operational evidence for ARB-013C-C03. The test validates the real path PC -> OneDrive -> EAGLE for AP-013C destination-verification acknowledgements and validates the EAGLE dry-run evaluator against the live transport namespace.

This evidence does not authorize any deletion.

## 2. Safety and operational boundaries

The OAT was executed under these invariants:

```text
COPY_ONLY
CleanupEligible = False
CleanupAuthorized = False
Deleted = 0
SourceFilesDeleted = 0
TransportFilesDeleted = 0
OverwritesPerformed = 0
```

The N.I.N.A. source on `D:\Images NINA\Target` was not part of cleanup scope. AP-013B remained the rollback baseline. The scheduled PC import task was temporarily disabled during the controlled pilot to prevent overlap with the manual AP-013C importer run.

## 3. PC baseline before AP-013C pilot

Observed on `WIN-QOOF3903TQS`:

```text
TransportRoot : C:\Users\MassimoMainini\OneDrive - Massimo Mainini\Manciano\DigitalStarGate-Transport
XISF          : 445
READY         : 445
ACK           : 0
PARTIAL       : 0
```

The existing scheduled import task completed successfully before the pilot (`LastTaskResult = 0`) but still referenced the prior AP-013B runtime clone and produced importer schema 1.1 without ACK fields. This confirmed that ACK generation had not yet been exercised by the productive scheduled path.

## 4. Controlled PC ACK pilot

A ten-asset transfer plan was derived from the current discovery plan and restricted to assets whose planned destination already existed on `F:\Astrofotografia`.

The AP-013C importer from the isolated worktree was executed manually with `MaxFilesPerRun=10`.

Observed importer summary:

```text
SchemaVersion             : 1.2
Mode                      : COPY_ONLY_ONEDRIVE_IMPORT_WITH_VERIFICATION_ACK
ReadyManifestsObserved    : 445
AlreadyImportedSkipped    : 10
DeferredNoPlan            : 435
DeferredPlanAction        : 0
DeferredTransportNotReady : 0
Requested                 : 0
CopiedVerified            : 0
SkippedIdentical          : 0
Failed                    : 0
AckCreated                : 10
AckExistingVerified       : 0
AckFailed                 : 0
SourceFilesDeleted        : 0
TransportFilesDeleted     : 0
OverwritesPerformed       : 0
```

Run ID:

```text
DSG-OD-IMPORT-df36905b-8885-4398-b157-00911975b2d3
```

Runtime evidence directory on PC:

```text
C:\Users\MassimoMainini\DSG-Inventory\AP013C-OAT\Import\DSG-OD-IMPORT-df36905b-8885-4398-b157-00911975b2d3
```

Post-run PC ACK inventory:

```text
ACK count = 10
```

## 5. Real OneDrive ACK reconvergence to EAGLE

After the PC pilot, metadata-only inventory on `EAGLE30154` observed:

```text
TransportRoot : C:\Users\PrimaLuceLab\OneDrive - Massimo Mainini\Manciano\DigitalStarGate-Transport
XISF          : 445
READY         : 445
ACK           : 10
PARTIAL       : 0
```

This is direct runtime evidence that the ten AP-013C ACK sidecars created on the PC converged through OneDrive into the EAGLE transport namespace.

## 6. Real EAGLE dry-run evaluator

The AP-013C evaluator was executed from an isolated EAGLE worktree pinned to the same code baseline.

Run ID:

```text
42c7209d-e43f-45c8-984b-ccd8987d2bc7
```

Runtime evidence directory on EAGLE:

```text
C:\Users\PrimaLuceLab\DSG-Inventory\AP013C-OAT\CleanupDryRun\20260904T150913Z-42c7209d-e43f-45c8-984b-ccd8987d2bc7
```

Observed summary:

```text
SchemaVersion       : 1.1
Mode                : DRY_RUN_NO_DELETE
Computer            : EAGLE30154
AssetsObserved      : 445
XisfObserved        : 445
ReadyObserved       : 445
AckObserved         : 10
TechnicalCandidates : 10
CleanupAuthorized   : 0
CleanupEligible     : 0
Blocked             : 435
Deleted             : 0
```

Observed reason-code distribution:

```text
ACK_MISSING                          : 435
TECHNICAL_CANDIDATE_POLICY_BLOCKED  : 10
```

## 7. Acceptance assessment

### Passed — real ACK generation

Ten destination-verified assets produced ten ACKs with zero ACK failures.

### Passed — real OneDrive convergence

All ten ACKs created on the PC were observed on EAGLE.

### Passed — fail-closed candidate semantics

Only the ten assets with converged ACK evidence became `TechnicalCandidate=True`. The remaining 435 assets were blocked by `ACK_MISSING`.

### Passed — policy authorization separation

Even for the ten technically proven assets:

```text
CleanupAuthorized = 0
CleanupEligible = 0
Deleted = 0
```

This validates the ARB-013C-C02 remediation in the live environment.

### Passed — no destructive side effects

Across the controlled PC importer pilot and EAGLE evaluator:

```text
SourceFilesDeleted = 0
TransportFilesDeleted = 0
OverwritesPerformed = 0
Deleted = 0
```

## 8. Related failure and recovery evidence

The real OAT in this record validates the AP-013C-specific ACK generation, OneDrive ACK reconvergence and live EAGLE dry-run classification path.

Additional failure/recovery coverage exists in two layers:

1. AP-013C CI includes synthetic incident-shaped divergence/reconvergence and fail-closed regression tests.
2. AP-013B operational acceptance already records real OneDrive interruption/recovery, PC restart recovery, EAGLE restart recovery, idempotent retry and no-delete/no-overwrite behavior for the unchanged transport/import foundation.

Those inherited AP-013B runtime results remain relevant because AP-013C does not replace the AP-013B COPY_ONLY transport foundation; it adds verification ACK evidence and a non-destructive evaluator above it.

No claim is made in this evidence that a new AP-013C-specific destructive or destination-outage production scenario was executed.

## 9. CI correlation

For runtime baseline `78b104c5732f353ca88ac0f3eced1de2ed1af14b`, GitHub Actions was verified successful for:

- AP-013C Verified Transport Cleanup;
- Developer Foundation;
- Validate documentation (no deploy);
- Genera manuale Word.

## 10. Disposition

**REAL OAT PASSED FOR DRY_RUN / NO_DELETE BEHAVIOR.**

The evidence is sufficient to demonstrate the live AP-013C ACK/reconvergence/evaluator path and fail-closed policy separation.

It does **not** authorize C8 productive cleanup. Retention/grace policy, crash-safe delete semantics and explicit ARB promotion remain separate mandatory gates.
