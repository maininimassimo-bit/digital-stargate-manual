# BKL-036-F5 — Live Read-Only OAT Evidence

| Field | Value |
|---|---|
| Date | 2026-09-22 |
| Owner / accountable | Massimo Mainini |
| Runtime | `dsg-observatory-status-relay` / `europe-west1` |
| Revision | `dsg-observatory-status-relay-00015-hak` |
| OAT mode | Read-only production read-back; no bearer token recorded |
| Result | `UNAVAILABLE` by policy; no partial score |

## Read-back

At `2026-09-22T18:48:31Z`, production Observatory Status returned HTTP `200`, quality
`CURRENT`, and advancing evidence for all six Observatory domains: weather `AVAILABLE`, dome
`CLOSED`, mount `IDLE`, camera `READY`, power `MAINS_PRESENT`, and network `ONLINE`.

At `2026-09-22T18:48:11Z`, production EAGLE Health returned HTTP `200`, quality `CURRENT`
and fresh signals. Its aggregate summary remained `UNKNOWN` with reason `POLICY_NOT_ACTIVATED`;
therefore `eagle_health` is not yet `COMPARABLE` for F5 scoring.

The command path check returned HTTP `404`. No command, broker, remediation or scheduler
action was used by this OAT, and no bearer token was printed or persisted.

## Gate decision

The six Observatory domains pass freshness and comparability. EAGLE Health passes transport,
schema and freshness, but fails the semantic comparability gate. The correct aggregate result is:

```text
score_status=UNAVAILABLE
score=null
reasons=EAGLE_HEALTH_NOT_COMPARABLE;NO_PARTIAL_SCORE_ALLOWED
```

Evidence is recorded in `docs/data/bkl-036-f5-live-oat-evidence-2026-09-22.json`. The next
action is to define and owner-authorize the EAGLE Health comparability policy, then repeat the
seven-domain OAT.
