# BKL-037 — Session Comparison & Benchmarking — Closure

| Campo | Valore |
|---|---|
| Identificativo | BKL-037-CLOSURE |
| Stato | CLOSED / ACCEPTED |
| Data | 10/09/2026 |
| Final merge | `97ced8a3c42a1480a2d588ec5573253614c88edd` |
| Final increment | F5 — Real-session acceptance and closure |

## Closure statement

BKL-037 è chiuso come capability read-only di confronto descrittivo fra sessioni scientifiche, con semantica fail-closed per comparabilità, provenance, unità e completeness.

Gli incrementi accettati sono:

- **F1** — source discovery e semantic contract;
- **F2** — canonical comparison read model;
- **F3** — deterministic multi-session comparison projection;
- **F4** — fail-closed read-only portal consumer;
- **F5** — persisted real-session projection, repository-side acceptance evidence e pubblicazione Pages.

## Accepted production slice

Il primo slice persistito confronta la mediana SQM di due sessioni reali governate:

- `2026-09-05_2026-09-06` — `20.57 mag/arcsec2`;
- `2026-09-07_2026-09-08` — `20.89 mag/arcsec2`.

La projection pubblicata espone esclusivamente statistiche descrittive e mantiene `READ_ONLY`, `acceptanceAuthority=false`, `actionAuthority=NONE`.

## Publication evidence

Il deployment GitHub Pages sul merge SHA `97ced8a3c42a1480a2d588ec5573253614c88edd` è completato con successo. L'artifact pubblicato contiene:

- `session-comparison/index.html`;
- `javascripts/session-comparison.js`;
- `styles/session-comparison.css`;
- `data/session-comparison-projection.json`.

Il published-site integrity gate ha validato il sito generato prima del deploy.

## Preserved boundaries

La closure non autorizza:

- ranking automatico fra sessioni;
- scientific quality score;
- acceptance threshold;
- SLA/SLO;
- recommendation AI;
- ricostruzione di PixInsight history `PARTIAL`/`UNAVAILABLE`;
- promozione di FWHM a arcsec senza unit provenance verificata;
- scritture AP-013/AP-014;
- comandi runtime, remediation o Safety Authority.

Le dimensioni diverse da SQM restano abilitate dal contratto architetturale, ma possono entrare in una projection pubblicata solo quando l'evidence soddisfa le regole di comparabilità F1/F2.

## Successor readiness

La sequenza di backlog stabilisce come successore **BKL-041 — Scientific Data Quality Score**, con dipendenze `BKL-029`, `BKL-037`, `BKL-045`.

Con la presente closure tutte e tre le dipendenze risultano accettate. BKL-041 può quindi essere promosso a package corrente, mantenendo separata la futura semantica di score dalla capability descrittiva BKL-037.

## Decision

**BKL-037 — CLOSED / ACCEPTED.**
