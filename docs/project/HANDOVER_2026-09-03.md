# Digital StarGate — Project Handover — 2026-09-03

| Campo | Valore |
|---|---|
| Stato | Current verified handover delta |
| Repository | `maininimassimo-bit/digital-stargate-manual` |
| Branch autorevole | `main` |
| Repository HEAD verificato prima del package | `d4feb68e89e39977d7b34b15acce69a0065dedb0` |
| Reporting release | `DigitalStarGate.Reporting 1.0.8` |
| Reporting merge | `0f9bffb5a9e6192dbf0302b563b10ec7e451bd8b` |
| Sessione recuperata | `2026-09-02_2026-09-03` |
| Capability successiva | BKL-030 EAGLE Health & Reliability |

## 1. Scopo

Questo handover registra il delta operativo del 03/09/2026 successivo a `HANDOVER_2026-09-02.md`. Non modifica i boundary architetturali o di safety approvati. GitHub resta la source of truth e lo stato deve essere riconfermato a ogni nuova sessione.

## 2. Incidente automatic session producer del 03/09/2026

Il Scheduled Task `Digital StarGate - Daily Session Upload` è partito alle 07:20 e ha caricato `DigitalStarGate.Reporting 1.0.7`. La discovery ha classificato `2026-09-02_2026-09-03` come `COMPLETE` con NINA, PHD2 e weather disponibili e il package è stato materializzato localmente, incluso SQM automatico.

La pubblicazione si è arrestata immediatamente dopo la creazione locale di `session/2026-09-02_2026-09-03`. L'evidence runtime ha mostrato come messaggio di failure il normale stderr Git `Switched to a new branch ...`. In Windows PowerShell 5.1, con `$ErrorActionPreference = 'Stop'`, il wrapper Git della 1.0.7 poteva promuovere quel normale stderr a errore terminante prima della valutazione di `$LASTEXITCODE`.

La working tree è rimasta con package non tracciato sul session branch; nessun commit o push era stato effettuato. Il package è stato preservato e diagnosticato senza reset distruttivi.

## 3. Drift discovery / packaging evidence

La diagnosi ha inoltre rilevato una divergenza tra discovery e copia evidence NINA/PHD2. La discovery usava la finestra sessione esatta, mentre il helper legacy di copia applicava una tolleranza di circa ±12 ore su `LastWriteTime`.

Per la sessione 02–03/09 ciò aveva incluso due file PHD2 della notte precedente, terminati il 02/09 alle 08:01, quindi estranei alla finestra osservativa corrente. Sono stati rimossi dal package prima della pubblicazione e dal manifest.

Il log NINA `20260901-081351-3.2.0.9001.10348-202609.log` è stato invece mantenuto: pur essendo stato creato il 01/09, contiene eventi reali del 02/09 fino alle 19:48:39 e appartiene quindi all'evidence della nuova sessione.

## 4. DigitalStarGate.Reporting 1.0.8

La release `1.0.8`, merge commit `0f9bffb5a9e6192dbf0302b563b10ec7e451bd8b` nel repository `maininimassimo-bit/DigitalStarGate.Reporting`, corregge i due difetti osservati:

1. `Invoke-DSGGitQuiet` rende non terminante il normale stderr dei processi Git sotto Windows PowerShell 5.1 e giudica l'esito tramite `$LASTEXITCODE`, preservando il singolo risultato strutturato di `Publish-DSGSession`;
2. il packaging NINA/PHD2 usa esattamente i file selezionati dalla discovery, eliminando il drift della finestra legacy ±12 ore e mantenendo comunque i log continuati quando `LastWriteTime` cade nella finestra reale.

La release mantiene il packaging SQM introdotto dalla 1.0.7 e non modifica Safety Authority, interlock o controllo device.

I gate della PR Reporting #5 sono risultati verdi prima del merge: Reporting Quality Gate #24, Runtime Hardening Regression #8 e Reporting NO_SESSION Regression #10.

## 5. Deployment runtime EAGLE30154

Il clone `C:\DigitalStarGate\DigitalStarGate.Reporting-src` è stato sincronizzato a `main` / `0f9bffb5a9e6192dbf0302b563b10ec7e451bd8b` e `VERSION = 1.0.8`.

`Install-DSGReporting.ps1` ha installato:

- modulo `DigitalStarGate.Reporting 1.0.8`;
- ModuleBase `C:\Users\PrimaLuceLab\Documents\WindowsPowerShell\Modules\DigitalStarGate.Reporting\1.0.8`;
- runtime config `C:\DigitalStarGate\Automation\reporting.config.psd1`;
- RepositoryRoot effettivo `C:\DigitalStarGate\digital-stargate-manual-ap14-runtime`.

Un nuovo processo Windows PowerShell `-NoProfile -NonInteractive -ExecutionPolicy Bypass` ha verificato `Import-DSGSession` e `Publish-DSGSession` versione `1.0.8`.

## 6. Recovery della sessione 2026-09-02_2026-09-03

Dopo la rimozione dei soli PHD2 estranei, il package conteneva:

- 3 log NINA, incluso il log continuato fino alle 19:48 del 02/09;
- 2 log PHD2 della sessione corrente;
- weather CloudWatcher;
- `raw/sqm/sqm-history.ndjson`;
- `raw/sqm/sqm-summary.json`.

SQM summary:

- quality `AVAILABLE`;
- valid samples `1295`;
- temporal coverage `0.9803`;
- min `8.91`;
- mean `18.1931`;
- max `20.84`;
- median `19.14` mag/arcsec²;
- source `AAG CloudWatcher SOLO HTTP / lightmpsas`;
- serial `2382`;
- firmware `5.88`.

La recovery ha invocato `Publish-DSGSession` 1.0.8 sulla branch già esistente, senza `-CreateBranch` e con `-Push`. Risultato runtime:

```text
SessionId = 2026-09-02_2026-09-03
Committed = True
Pushed = True
COUNT = 1
```

Commit sessione: `0023553bf300a1536663a6ee01f7d6a5c3745514`.

Il workflow `Promote session package to main` #14 è terminato `success`. Le analytics sono state rigenerate e `main` ha raggiunto `d4feb68e89e39977d7b34b15acce69a0065dedb0` con `chore(analytics): refresh 2026-09-02_2026-09-03 projections`.

Le projection riportano M 27, configurazione `C8_QHY695A_BIN1`, 78 light started, 77 completed, 1 failed, completion `98.72%`, RMS total `0.244 arcsec`, SQM `AVAILABLE`, 1295 campioni/coverage `0.9803`, 40 autofocus con 3 failure, 160 dither e severity `YELLOW`.

## 7. Stato finale runtime

Dopo la promozione, EAGLE è stato riallineato:

- branch `main`;
- working tree clean;
- fast-forward fino al `main` remoto;
- `HEAD...origin/main = 0 0`.

Il pull ha materializzato localmente package, normalized metrics, report Markdown/PDF, cataloghi scientifici e analytics della sessione 02–03/09.

## 8. Invarianti operativi

- Il preflight deve partire esclusivamente da runtime clone clean, branch `main`, sincronizzato con `origin/main`.
- Il Scheduled Task continua a usare preflight -> launcher -> `DigitalStarGate.Reporting`.
- Reporting runtime corrente è 1.0.8.
- NINA/PHD2 packaging deve seguire la discovery esatta; non reintrodurre la tolleranza legacy ±12 ore.
- `Publish-DSGSession` deve restituire un solo risultato strutturato anche sotto Windows PowerShell 5.1.
- Nessun force push/reset distruttivo è ammesso come normale recovery.
- SQM non è Safety Authority.
- Evidence scientifica, health e projection non sostituiscono gli interlock locali/fisici.
- `NO_SESSION` resta un outcome valido e non un errore.

## 9. Debito tecnico residuo

Resta aperto il launcher hardening per garantire il restore-to-main anche quando un'eccezione avviene dopo la creazione del session branch e prima del cleanup. L'incidente del 03/09 ne ha fornito evidence reale. La 1.0.8 corregge la causa Git osservata ma non sostituisce un `finally` governato nel launcher.

La copertura quality gate Reporting deve continuare a essere confrontata con la copertura storica pre-1.0.7: i regression test 1.0.8 coprono i due incidenti correnti, ma il ripristino completo degli altri check storici resta un hardening separato finché non verificato esplicitamente.

## 10. Prossimo passo governato

Con Reporting 1.0.8 installato, la sessione 02–03/09 recuperata/promossa e il runtime EAGLE nuovamente `main / clean / 0 0`, il prossimo lavoro di capability è **BKL-030 EAGLE Health & Reliability**.

Ripartire da D1/D2 source discovery read-only su `EAGLE30154` tramite `scripts/telemetry/Inspect-EagleHealthSources.ps1`. Nessuna soglia HEALTHY/DEGRADED/CRITICAL, remediation automatica o uso come Safety Authority è autorizzato prima della classificazione delle source/evidence.
