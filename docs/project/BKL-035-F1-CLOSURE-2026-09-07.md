# BKL-035 F1 — Closure Record — 2026-09-07

| Field | Value |
|---|---|
| Package | BKL-035 — Target Knowledge Base |
| Increment | F1 — Source Discovery & Semantic Contract |
| Status | CLOSED / ACCEPTED |
| PR | #106 |
| Merge commit | `a21b88b5bc8049675eb89d5d98888b3fb3ce898f` |
| Runtime impact | None |
| Safety impact | None |
| Next increment | F2 — Machine-Readable Target Identity/Relation Schema + bounded fixture |

## Acceptance evidence

F1 established a deterministic, bounded source-discovery and semantic contract for a target-centric projection over existing governed Digital StarGate sources.

The accepted contract defines:

- exact repository source paths and record locators;
- fact-class-specific source precedence;
- canonical target identity semantics;
- deterministic identifier/name normalization boundaries;
- repository-local identity fallback conditions;
- `validated`, `incomplete`, `conflicted`, and `unknown` identity states;
- collision/disagreement fail-closed behavior;
- canonical-name selection;
- bounded relation vocabulary for sessions, SQM, setup, assets and processing provenance;
- BKL-044 Citation/Provenance/Confidence/authority inheritance;
- no broad ingestion, graph/vector/RAG/runtime AI or Safety Authority change.

## Review evidence

Independent Architecture Review Board sequence:

- initial review — REWORK REQUIRED, 93/100;
- first re-review — REWORK REQUIRED, 98/100 due to exact Session Catalog locator mismatch;
- final re-review on `5efeb518622467ba8c47588a94c80bdeb60d3775` — APPROVED, 100/100.

The final review closed all findings, including the repository-true `sessions[].sessionId` and `sessions[].sourceMetricsPath` locator semantics.

## Exact-head CI before merge

On head `5efeb518622467ba8c47588a94c80bdeb60d3775`:

- Developer Foundation #1010 — SUCCESS;
- Validate documentation #619 — SUCCESS;
- Genera manuale Word #1044 — SUCCESS.

## Post-merge validation

On merge commit `a21b88b5bc8049675eb89d5d98888b3fb3ce898f`:

- Developer Foundation #1011 — SUCCESS;
- Validate documentation #620 — SUCCESS;
- Genera manuale Word #1045 — SUCCESS;
- Deploy MkDocs artifact to GitHub Pages #699 — SUCCESS.

## Permanent properties transferred to F2

F2 must preserve these F1 decisions exactly:

1. BKL-035 remains a projection, not a new scientific authority.
2. Exact governed identifiers outrank display/read-model labels.
3. Different exact governed identifiers are never automatically merged.
4. Fuzzy/AI/coordinate-nearness matching is not authorized.
5. A repository-local target key may be created only from deterministic governed evidence when no exact catalog identifier exists and no incompatible evidence is present.
6. Identity states remain explicit and fail closed.
7. Session/SQM/setup/asset/processing relations must trace to accepted source records.
8. SQM evidence remains session-scoped unless a later derived statistic declares method and provenance.
9. Asset/processing relations require acceptable governed reconciliation state.
10. Citation/Provenance/Confidence/lifecycle semantics remain governed by BKL-044.

## F2 guardrails

F2 may introduce only:

- a versioned machine-readable schema for Target Knowledge Identity and relation records;
- a bounded governed fixture within the F1 limits (maximum 5 identities / 10 session relations);
- deterministic schema/contract validation and negative tests;
- Developer Foundation integration.

F2 must not introduce external catalog enrichment, broad ingestion, persistent graph/vector storage, RAG, AI inference authority, runtime EAGLE behavior, observatory command paths, automatic remediation or Safety Authority coupling.

## Closure decision

BKL-035 remains `In Progress` because F2-F4 are still pending. F1 alone is CLOSED / ACCEPTED. The next governed increment is **F2 — Machine-Readable Target Identity/Relation Schema + bounded fixture**, starting from verified `main` baseline `a21b88b5bc8049675eb89d5d98888b3fb3ce898f`.