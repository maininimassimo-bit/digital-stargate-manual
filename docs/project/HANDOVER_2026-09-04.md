# Digital StarGate — Project Handover — 2026-09-04

| Campo | Valore |
|---|---|
| Stato | Current verified handover — BKL-030 closure |
| Repository | `maininimassimo-bit/digital-stargate-manual` |
| Branch autorevole | `main` |
| BKL-030 closure merge | `eeba3372d8d788322f52553091f8d63cef5f9e68` |
| Reporting runtime | `DigitalStarGate.Reporting 1.0.8` |
| Capability chiusa | BKL-030 EAGLE Health & Reliability Telemetry — CLOSED / ACCEPTED |
| Prossima capability | BKL-015 Knowledge Graph machine-readable foundation |
| Raw transport baseline | AP-013B `COPY_ONLY`; AP-013C DRY_RUN/NO_DELETE integrato |

## 1. Scopo

Questo handover sostituisce, per la continuità operativa del 04/09/2026, le sezioni precedenti dello stesso documento che descrivevano BKL-030 come `In Progress`. GitHub resta la source of truth. Le evidence e le baseline storiche non vengono riscritte retroattivamente.

## 2. BKL-030 — stato finale

BKL-030 EAGLE Health & Reliability Telemetry è **CLOSED / ACCEPTED** dopo i gate G1-G8.

La chiusura governance è stata integrata in `main` con PR #90 e merge commit `eeba3372d8d788322f52553091f8d63cef5f9e68`. Prima del merge i workflow Developer Foundation, Validate documentation e Genera manuale Word risultavano verdi sul commit della PR.

G8 mantiene i boundary approvati:

- telemetria EAGLE read-only;
- nessuna remediation automatica;
- nessun command endpoint;
- browser senza ingest token;
- Safety Authority fisica/locale separata e autorevole;
- `summary.state=UNKNOWN` e `summary.reason=POLICY_NOT_ACTIVATED` finché non esiste una health policy governata;
- stale/missing/malformed evidence deve degradare fail-closed, non produrre uno stato sano simulato.

## 3. Observatory Status e Cloud Run

G7 ha esteso il relay Cloud Run esistente con un canale separato `/v1/eagle-health`, senza alterare il contratto `/v1/observatory-status`.

La revisione production verificata è `dsg-observatory-status-relay-00005-rof`. Il canary e il cutover hanno verificato sia EAGLE Health sia compatibilità N.I.N.A.; il relay ha mantenuto contatori rejected pari a zero durante l'OAT registrato.

La pagina `Observatory Status` è stata verificata operativamente con dati EAGLE popolati. La UI è consumer: non invoca probe host e non possiede token di ingest.

## 4. Publisher EAGLE permanente

Su `EAGLE30154` è stato installato e avviato lo Scheduled Task:

`DigitalStarGate-EagleHealthTelemetry`

Configurazione operativa:

- principal `PrimaLuceLab`;
- logon `S4U`;
- run level Highest;
- cadenza 1 minuto;
- `MultipleInstances=IgnoreNew`;
- wrapper `scripts/telemetry/Invoke-EagleHealthTelemetryPublish.ps1`;
- repository runtime `C:\DigitalStarGate\digital-stargate-manual-ap14-runtime`;
- secret `C:\DigitalStarGate\TelemetryRuntime\secrets\ingest-token.dpapi`, DPAPI LocalMachine;
- projection pubblica read-only;
- nessuna remediation.

La verifica runtime ha osservato projection `CURRENT`, host `EAGLE30154`, cinque segnali pubblicati e invarianti `automatic_remediation=false`, `safety_authority=OUTSIDE_SCOPE`.

## 5. Vincolo operativo EAGLE — branch main

Il runtime operativo EAGLE **deve rimanere sul branch `main`**. Questo è un prerequisito di continuità per la telemetria e soprattutto per l'importazione automatica delle sessioni scientifiche del giorno successivo.

Path autorevole:

`C:\DigitalStarGate\digital-stargate-manual-ap14-runtime`

Controllo pre-sessione/pre-import minimo:

1. `git branch --show-current` deve restituire `main`;
2. `git status --short` deve essere vuoto;
3. `git fetch origin`;
4. `git rev-parse HEAD` deve coincidere con `git rev-parse origin/main`.

Worktree feature, OAT o branch temporanei non devono sostituire il runtime operativo. Le prove isolate devono usare worktree separati.

Al momento dell'ultima verifica operativa prima della closure, EAGLE era su `main`, working tree pulito e `HEAD == origin/main == a15d85b27ebfbe8a6488330920d10dda8db79a78`. Il merge governance successivo `eeba3372...` non richiede aggiornamento immediato del runtime durante una finestra operativa: l'allineamento va effettuato in modo controllato prima di una futura modifica runtime.

## 6. Scientific session / raw transport

`DigitalStarGate.Reporting 1.0.8` resta la baseline runtime scientifica EAGLE.

AP-013B resta il rollback comportamentale `COPY_ONLY`. AP-013C ha completato l'incremento governato DRY_RUN/NO_DELETE: nessuna cancellazione produttiva C8/source è autorizzata.

Restano validi i guardrail:

- evidence incompleta o UNKNOWN => no-delete;
- `F:\Astrofotografia` è destinazione autorevole e non cleanup target;
- il transport OneDrive e la source N.I.N.A. non devono essere cancellati sulla sola export completion;
- cleanup produttivo futuro richiede decisione separata.

## 7. Evidenze operative BKL-030 da non reinterpretare

Durante le prove EAGLE sono stati osservati segnali storage e Windows Time. Questi sono dati osservati, non una policy di severity.

In particolare, nessuna soglia HEALTHY/DEGRADED/CRITICAL è stata approvata e il servizio Windows Time osservato `Stopped` non autorizza remediation automatica. Qualunque policy o remediation futura richiede nuovo package governato.

## 8. Safety e security boundary

Invariati:

- local physical Safety Authority indipendente;
- telemetria, health, AI, portal e Cloud Run non sono Safety Authority;
- nessun command endpoint nel relay;
- nessun token nel browser;
- secret ingest non deve essere stampato o salvato plaintext;
- outage Cloud Run non deve influenzare N.I.N.A., collector locale, mount, dome, weather safety o interlock fisici.

## 9. Continuity documents

Documenti correnti:

- `AI_BOOTSTRAP.md`;
- `docs/project/HANDOVER_2026-09-04.md`;
- `docs/project/CURRENT_TECHNICAL_BASELINE_2026-09-04.md`;
- `docs/project/BACKLOG.md`;
- `.github/roadmap/roadmap-source.json`;
- G6/G7/G8 review, closure ed evidence BKL-030.

Le baseline datate precedenti restano record storici.

## 10. Debito e rischi residui

- C8 / cleanup produttivo source resta non autorizzato;
- health severity policy non attivata;
- automatic remediation non autorizzata;
- mantenere il runtime EAGLE su `main` e pulito;
- verificare la riuscita dell'import sessione successiva come normale evidence operativa, senza confonderla con una riapertura BKL-030.

## 11. Handoff verso BKL-015

BKL-015 può iniziare solo dopo merge e workflow verdi di questo aggiornamento handover.

Il package BKL-015 deve partire da repository truth corrente e non deve modificare implicitamente i boundary chiusi da BKL-030. In particolare Knowledge Graph/AI non devono diventare Safety Authority, non devono introdurre remediation EAGLE e devono distinguere evidence, observation, inference e recommendation.
