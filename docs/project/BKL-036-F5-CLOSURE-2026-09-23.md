# BKL-036-F5 — Post-Merge Acceptance and Closure

| Field | Value |
|---|---|
| Date | 2026-09-23 |
| Status | CLOSED / ACCEPTED / POST-MERGE VERIFIED |
| Owner / accountable | Massimo Mainini |
| Repository baseline | `main` at `ba891bc2` |
| Scope | Live descriptive EAGLE Health projection, read-only only |
| Runtime authority | `NONE` |
| Safety authority | `NONE` in the projection; local physical interlocks remain authoritative |

## Closure decision

BKL-036-F5 is accepted for the bounded live read-only scope. The owner-authorized policy is
active and the production projection is current and healthy. This acceptance does not create a
broker, scheduler decision path, command path, remediation, automatic target selection or
Safety Authority.

## Live evidence

The production Cloud Run relay is `dsg-observatory-status-relay` in `europe-west1`, with 100%
traffic on `dsg-observatory-status-relay-00019-keq` and rollback retained at
`dsg-observatory-status-relay-00005-rof`.

The final read-only evidence and live read-back record:

```text
GET /v1/eagle-health: HTTP 200
quality=CURRENT
state=HEALTHY
score=100
reason=ALL_REQUIRED_SIGNALS_HEALTHY
required signals=5/5
host=EAGLE30154
```

Windows Time is active and synchronized through `time.windows.com`. The scheduled task
`DigitalStarGate-EagleHealthTelemetry` is aligned to the AP-008 worktree and returned
`LastTaskResult=0`. The bearer token remained in the existing DPAPI path and was never printed
or requested.

## Acceptance matrix

| Gate | Result | Evidence |
|---|---|---|
| Live EAGLE Health `HEALTHY/100` | PASS | `docs/architecture/validation/evidence/AP008-BKL036-F5-FINAL-LIVE-READONLY-EVIDENCE-2026-09-22.md`; production GET read-back |
| GitHub Pages consumer | PASS | `https://maininimassimo-bit.github.io/digital-stargate-manual/status/`; rendered `✅ 100/100`, `5/5 segnali obbligatori correnti`, `Cloud Run relay · EAGLE30154 · live read-only`, `ALL_REQUIRED_SIGNALS_HEALTHY` |
| Fail-closed `DEGRADED` / `UNAVAILABLE` | PASS | `docs/architecture/validation/BKL-036-F5-EAGLE-HEALTH-POLICY-2026-09-22.md`; missing, stale or uncomputable required data yields `UNAVAILABLE`, threshold breach yields `DEGRADED`, and no partial score is emitted |
| ARB / Release Quality | PASS WITH OWNER-WITNESSED WAIVER | `docs/architecture/validation/AP008-ARB-ReReview-AI-Assisted-2026-09-22.md`; `docs/architecture/validation/AP008-Approval-Attestations-2026-09-22.md` |
| Command boundary | PASS | `/v1/command` remains `404`; `command_authority=NONE` |
| Runtime and safety boundary | PASS | `runtime_event_published=false`; no broker, remediation or Safety Authority |

## Policy confirmation

- `cpu`, `memory`, `storage`, `uptime` and `time_sync` are the five mandatory signals.
- `HEALTHY` is emitted only when all five are current and valid with no threshold breach.
- CPU above 90% or memory below 20% across the governed five-sample window yields `DEGRADED`.
- `C:` or `D:` below 20% free yields immediate `DEGRADED`; exactly 20% remains normal.
- Missing, stale, malformed or uncomputable mandatory evidence yields `UNAVAILABLE`.
- `UNAVAILABLE` is never converted to zero and never produces a partial score.

## Governance and residual conditions

ARB/RQ review and the security/trust and ARB attestations remain owner-witnessed under the
recorded waiver. The accepted residual risks remain secret lifecycle, IAM/bucket review,
public read exposure/CORS, log redaction and supply chain. They do not expand the approved
scope. Any future production promotion, authority change or removal of these waivers requires
a new governed decision and fresh evidence.

## Final boundary

The score is descriptive and read-only. It is not a readiness verdict, go/no-go decision,
command authorization or Safety Authority. No new Cloud Run deployment or promotion was needed
for this acceptance.
