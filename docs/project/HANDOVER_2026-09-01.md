# Digital StarGate — Project Handover — 2026-09-01

| Campo | Valore |
|---|---|
| Stato | Current verified handover baseline |
| Repository | `maininimassimo-bit/digital-stargate-manual` |
| Branch autorevole | `main` |
| Baseline verificata prima di questo aggiornamento | `7d69fbfff818c2cedb45126a48f74905ba58c0ab` |
| Bootstrap | `/AI_BOOTSTRAP.md` |
| Capability completata | BKL-029 SQM Sky Quality Telemetry & Scientific History |
| Prossima capability | BKL-030 EAGLE Health & Reliability |

## 1. Scopo

Questo handover aggiorna la baseline del 30/08/2026 e deve essere letto come stato corrente. `HANDOVER_2026-08-30.md` resta un record storico e non deve essere usato per determinare la prossima capability.

Il repository GitHub resta l'unica fonte autorevole: branch, HEAD, workflow, Architecture Package, ADR, evidence, backlog e projection devono essere riconfermati a ogni nuova sessione.

## 2. Stato consolidato al 01/09/2026

- BKL-029 è `Done` con source SQM reale verificata, realtime telemetry, historical provenance, session analytics, catalog projection, E2E/CI e runtime OAT.
- La PR #68 ha consolidato la capability BKL-029; merge commit `0ebf04ec0ba1c4a1236f2a52e8a7e44abe0c6441`.
- La sessione reale `2026-08-31_2026-09-01` / M 27 è stata rigenerata sulla pipeline canonica e contiene metadata scientifici risolti e storico SQM.
- La source SQM scientifica è AAG CloudWatcher SOLO HTTP `lightmpsas`; SQM resta una metrica scientifica e **non** è Safety Authority.
- La projection canonica della sessione M 27 include target, coordinate J2000, configurazione `C8_QHY695A_BIN1`, telescopio `Celestron C8 XLT`, camera `QHY695A`, integrazione/guiding e SQM.
- Il Scientific Session Catalog e il Scientific Observation Index ricevono i dati dalla pipeline canonica; i dataset JSON sono projection ricostruibili, non source primaria.
- Le correzioni successive a BKL-029 hanno eliminato drift di presentazione senza cambiare il boundary architetturale o safety.

## 3. Correzioni di projection/presentation successive a BKL-029

### 3.1 Refresh della sessione M 27

La sessione già importata è stata rianalizzata dopo l'introduzione dello schema canonico, così da rigenerare `normalized/session-metrics.json`, history e projection portale con target/equipment/coordinate/SQM correnti.

### 3.2 Normalizzazione timezone Home

La pipeline Home è stata corretta per normalizzare timestamp legacy offset-naive e timestamp timezone-aware in UTC prima dell'ordinamento. La correzione evita il `TypeError` emerso durante il refresh della Home dopo la rigenerazione M 27.

PR #70 è stata mergiata con merge commit `f4b607522b7f710459be00a72c89af75ca4c9635`; la relativa pipeline Pages è risultata verde.

### 3.3 Presentazione SQM nel catalogo scientifico

Lo SQM era già storicizzato e presente nel catalogo JSON/Observation Index, ma non era esposto dalla card del Scientific Session Catalog. La presentation è stata riallineata affinché il dato SQM scientifico disponibile sia mostrato all'utente senza trasformarlo in un segnale Safety.

### 3.4 Home — ultima sessione dinamica

La Home non deve dipendere da metadata statici per determinare il target dell'ultima sessione. Target, coordinate ed equipment della sezione ultima sessione sono proiettati dai dati canonici session/history, con registry metadata solo come fallback governato.

La PR #72 è stata mergiata in `main` con merge commit `7d69fbfff818c2cedb45126a48f74905ba58c0ab`.

Sul merge SHA sono stati verificati `SUCCESS`:

- Validate Digital StarGate History #30;
- Developer Foundation #875;
- Genera manuale Word #882;
- Deploy MkDocs artifact to GitHub Pages #646.

Nel workflow Pages #646 sono risultati `SUCCESS` anche `Refresh governed latest observation`, `Refresh governed homepage projections`, build MkDocs, site-integrity check, upload artifact e deploy GitHub Pages.

## 4. Contratto scientifico corrente

Per la sessione M 27 la baseline canonica verificata comprende:

- session id `2026-08-31_2026-09-01`;
- target `M 27`;
- RA `299.9°`;
- DEC `+22.72111111°`;
- epoch `J2000`;
- coordinate source `nina-log` / projection canonica;
- configuration `C8_QHY695A_BIN1`;
- telescope `Celestron C8 XLT`;
- camera `QHY695A`;
- SQM median `18.66 mag/arcsec²`;
- SQM mean `17.5395 mag/arcsec²`;
- SQM valid samples `1301`.

Le statistiche SQM storiche comprendono start/end/min/max/mean/median/valid samples/temporal coverage/source/quality secondo il contratto BKL-029.

## 5. Invarianti da preservare

- GitHub repository as source of truth.
- Scientific Data Engine come access layer condiviso del portale scientifico.
- Dataset JSON e Home/catalog data sono projection, non authority.
- SQM è scientific telemetry/history, mai Safety Authority.
- Safety Authority locale/fisica resta indipendente da portale, analytics, health e AI.
- Nessun comando diretto portale/AI verso apparati nella baseline corrente.
- Niente last-known-good presentato come current quando freshness/source non è valida.
- Collector EAGLE leggeri e inizialmente read-only.
- Nessuna soglia health inventata senza evidence.

## 6. Stato documentale e gerarchia

I documenti datati mantengono valore storico. Quando una sezione di stato di un documento precedente contrasta con questo handover o con repository/evidence più recenti, prevalgono nell'ordine:

1. repository/HEAD e workflow verificati;
2. Architecture Package, ADR, contract ed evidence approvati;
3. questo handover corrente;
4. backlog/roadmap autorevoli correnti;
5. documenti di contesto storici;
6. projection JSON;
7. conversazioni.

In particolare `HANDOVER_2026-08-30.md`, le sezioni di stato datate di `ENTERPRISE_ARCHITECTURE_CONTEXT.md` e `REPOSITORY_KNOWLEDGE_MAP.md` non devono essere interpretate come stato operativo corrente quando riportano AP-014/BKL-029 come pianificati.

## 7. Prossimo passo governato — BKL-030

BKL-030 EAGLE Health & Reliability è `Ready`.

Primo passo: D1/D2 source discovery read-only su `EAGLE30154` con:

`scripts/telemetry/Inspect-EagleHealthSources.ps1`

La discovery deve inventariare solo source osservabili e privilegi richiesti. Non deve riavviare processi o host, cambiare task/registry/update/power/network, resettare device o introdurre remediation automatica.

Evidence prevista sotto:

`C:\DigitalStarGate\TelemetryEvidence\eagle-health-source-discovery-YYYYMMDD-HHMMSS\`

Dopo D1/D2: classificazione source/privilegi, D3 overhead pilot e D4 failure model prima di definire soglie o score operativi.

## 8. Startup checklist nuova chat

1. leggere `AI_BOOTSTRAP.md`;
2. leggere questo `HANDOVER_2026-09-01.md`;
3. verificare HEAD corrente di `main` e commit successivi alla baseline indicata;
4. verificare workflow Developer Foundation, History, Pages/documentation e Word applicabili;
5. leggere `ENTERPRISE_ARCHITECTURE_CONTEXT.md` e `REPOSITORY_KNOWLEDGE_MAP.md` come contesto strutturale, applicando le note di supersessione temporale di questo handover;
6. leggere `BACKLOG.md`, functional roadmap, roadmap source, technical debt e decision log;
7. aprire Architecture Package/ADR/evidence direttamente coinvolti;
8. se non esiste drift o gate rosso, proseguire con BKL-030 D1/D2.

## 9. Definition of safe handover

Una nuova sessione deve distinguere senza ambiguità: Done vs Planned; source primaria vs projection; runtime evidence vs design; SQM scientifico vs Safety; health vs Safety; metadata canonici vs fallback; stato corrente vs documento storico. Qualunque divergenza deve essere registrata e risolta prima di avanzare alla capability successiva.
