# AP-008 — Bounded Read-Only Integration Closure

| Field | Value |
|---|---|
| Package | AP-008 Enterprise Integration Architecture |
| Status | Bounded scope closed / accepted / post-merge verified |
| Owner / accountable | Massimo Mainini |
| Production revision | `dsg-observatory-status-relay-00015-hak` |
| Rollback revision | `dsg-observatory-status-relay-00005-rof` |
| Portal integration | GitHub Pages live read-only consumer |
| Waiver | Owner-witnessed security/trust and ARB waiver retained as historical condition |

## Closure decision

AP-008 is closed for the bounded read-only scope: Observatory Status, EAGLE Health and
SessionCompleted shadow transport are live, continuously published/readable and fail-closed.
The public portal consumes production GET projections without ingest credentials.

Continuous verification recorded HTTP `200` and `CURRENT` for Observatory Status and EAGLE
Health with advancing timestamps. The command route remained HTTP `404`. GitHub Pages build,
integrity validation and deployment succeeded after the AP-008 evidence merge.

## Retained boundaries

This closure does not authorize a broker, scheduler-driven command path, device control,
remediation, automatic target selection, `runtime_event_published=true`, Safety Authority or
replacement of BKL-032. The Windows Scheduled Task is only a read-only telemetry publisher.
Local physical interlocks remain authoritative.

## Successor

BKL-036-F5 is opened as a separate owner-authorized package for live descriptive health-score
compatibility. It must pass its own contract, evidence, ARB, Release Quality and OAT gates.
