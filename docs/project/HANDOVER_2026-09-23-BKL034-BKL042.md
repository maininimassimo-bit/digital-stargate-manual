# Handover — BKL-034 → BKL-042

## Stato consegnato

BKL-034 è CLOSED / ACCEPTED / POST-MERGE VERIFIED dopo la consegna del consumer portale bounded read-only della Scientific Image Gallery. Ogni projection collega immagine, sessione, target, provenance e source evidence con stato/freshness espliciti.

## Stato repository e portale aggiornato

- `main` è allineato al commit `ad74a35d` (`docs(handover): record final governed head`), che include il riallineamento finale di handover e baseline sullo stato governato;
- il foglio globale `docs/styles/theme-contrast.css` riallinea sfondi, testo, tabelle,
  admonition, codice, citazioni, navigazione e variabili Material nel tema chiaro;
- le superfici legacy di Status, Roadmap, Operations, Architecture e homepage sono
  leggibili in tema chiaro senza alterare il tema scuro o i colori semantici delle
  evidenze verde/rosso/ambra;
- la verifica pubblica è stata eseguita su Status, Roadmap e Scientific Image Gallery
  in tema chiaro; il tema scuro resta preservato;
- workflow finali sulla revisione governata: Pages `35894429499`, Validate documentation
  `35894429242`, Developer Foundation `35894429094`, BKL-031 F6 governance
  `35894429214`, Word generation `35894429272`, F4-C `35894429419` e F4-D
  `35894429262`, tutti con esito `SUCCESS`.

## Baseline

- authority: `projection`;
- command authority: `NONE`;
- safety authority: `NONE`;
- nessuna image mutation, processing execution, command, remediation o scheduler decisionale;
- nessuna promozione a runtime ingest, UI operativa o provider/AI apply.

## Stato aggiornato — BKL-042 F6 portal ingress

BKL-042 F6 portal-ingress preesistente ha formal acceptance owner-witnessed in
`docs/architecture/validation/BKL-042-F6-PORTAL-OAUTH-OAT-EVIDENCE-2026-09-23.md`.
Il primo retrieval/provenance increment limitato alle proiezioni sessioni e target è
stato integrato con PR #355 (`b4d45c12`). L'OAT owner ha individuato un mancato match
fra `m27` e `M 27`; PR #357 (`7be9fd0b`) ha corretto la normalizzazione e versionato il
metodo v2. Il relay è ora su `dsg-bkl042-ai-relay-00007-wrm` (100%; rollback
`00006-ktv`), con health gateway `READY`.

La nuova OAT owner-witnessed autenticata sul metodo v2 è PASS il 24/09/2026: `ANSWERED`,
cinque sessioni M 27 citate e valori verificati sulla projection, correlation
`bkl042-pages-ad1e6d8b-499a-460e-a4ea-c6546a1ba419`; le autorità restano `NONE`. Il
precedente `INSUFFICIENT_EVIDENCE` non ha chiamato il provider, ma ha consumato uno slot
applicativo. Restano pending formal bounded acceptance owner e decisione sul perimetro
delle classi source residue F1: l'incremento non dichiara copertura completa. Storage/upload runtime,
processing, tool, PixInsight apply, command path, remediation, scheduler decisionale e
Safety Authority restano esclusi.

## Prossimo gate

BKL-042 — registrare formal bounded acceptance owner e decidere se chiudere il package
con copertura iniziale sessioni/target e limiti dichiarati oppure promuovere un gate
separato per le altre fonti F1 eleggibili.

La correzione del tema è un adeguamento trasversale di presentazione e non modifica
contratti, provider, traffico esterno, runtime authority, command path, remediation,
scheduler decisionale o Safety Authority.
