# Handover — AP-008 closure and BKL-036-F5

| Field | Value |
|---|---|
| Date | 2026-09-22 |
| Owner / accountable | Massimo Mainini |
| Repository baseline | `main` after AP-008 continuous read-only evidence merge |
| AP-008 status | Bounded read-only integration closed / accepted |
| BKL-036 status | F1-F4 closed; F5 policy activated and live; current score `HEALTHY=100` |
| Runtime status | Observatory Status and EAGLE Health continuously read-only |
| Safety status | Local interlocks only; no Safety Authority transfer |

## Completed

- Cloud Run production read-only rollout is at revision `00019-keq`;
- portal production consumer is deployed and verified;
- EAGLE30154 Scheduled Task publishes Observatory Status continuously;
- EAGLE Health policy is active, read-only and fail-closed;
- live production read-back is `200`, `CURRENT`, `HEALTHY`, `score=100`;
- `DigitalStarGate-EagleHealthTelemetry` points to the AP-008 worktree and returns `LastTaskResult = 0`;
- `/v1/command` remains `404`;
- AP-008 bounded closure evidence is merged through PR #345.

## Current successor

BKL-036-F5 defines the live read-only score contract. The owner-confirmed policy defines five
required signals, immediate disk degradation for `C:`/`D:`, five-minute CPU/memory windows,
fresh time sync, and deterministic `HEALTHY=100` / `DEGRADED=50` scoring. The live OAT
reached five valid CPU/memory samples and fresh Windows time synchronization evidence.

## Final AP-008 / F5 evidence

- EAGLE Health POST returned `202` with idempotency correlation;
- EAGLE Health GET returned `200`, `quality=CURRENT`, `state=HEALTHY`,
  `reason=ALL_REQUIRED_SIGNALS_HEALTHY`, `score=100`;
- no bearer token was printed or copied;
- no command, broker, remediation or Safety Authority semantics were introduced.

## Do not change

Do not enable `runtime_event_published=true`, broker, command path, remediation, automatic target
selection or Safety Authority coupling. The numeric score remains descriptive/read-only and
does not grant command, remediation or Safety Authority semantics.
