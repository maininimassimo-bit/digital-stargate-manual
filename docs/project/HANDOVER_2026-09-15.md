# Digital StarGate — Handover 15/09/2026

| Campo | Valore |
|---|---|
| Identificativo | DSG-HO-2026-09-15 |
| Stato | **CURRENT — F3-A3 PARTIAL OWNER DECISION / GCP REPOSITORY SCAFFOLDING / F3-OD05 PENDING** |
| Repository | `maininimassimo-bit/digital-stargate-manual` |
| Baseline verificata | `main@527b298094b07e5a00317e60cab3abefed7a5759` |
| Capability corrente | BKL-031 — Observation Planner intelligente |
| Incremento corrente | F3-A3 partial decision + isolated Google Cloud Terraform/WIF scaffolding |
| Mandato | `DSG-AEM-001` v1.1 ACTIVE |
| Ruleset exception | `W-DSG-AEM-RULESET-001` ACTIVE / CONDITIONED |
| PC Principale / EAGLE | Nessuna azione richiesta |

## 1. Completed

PR #210 reconciled D5 acceptance and merged with expected-head control as `a1d2d84f9f787c247516bc825cb83d2bc15a43f5`, with 7/7 post-merge workflows. Its underlying D5 promotion was PR #209, merged with expected-head control as `bc4307c2042a45985622044e11631421de5b2c3d`. Exact-head CI passed 5/5; Documentation recorded `ACCEPTED WITH OBSERVATION`; the AI-assisted, process-separated ARB recorded `APPROVED WITH CONDITIONS — 99/100`; Release Quality recorded `CONDITIONALLY READY FOR MERGE`. All 7 post-merge workflows, including GitHub Pages, succeeded. The reviews are not equivalent to independent human approval.

The protected receipt and separate `APPROVED` envelope are integrated while the historical DRAFT and assignment payload/digest remain unchanged. Exact protected digests, internal identifiers and site facts are intentionally omitted from this handover.

## 2. Current authority state

- approved setup baseline: present;
- F3-A1 protected Site Authority: APPROVED and repository-resolver eligible for authorized callers within validity;
- F3-A2-D3 assignment source/roles/validity decisions: complete;
- protected assignment decision evidence: integrated;
- CurrentSetupAssignment: protected historical DRAFT retained; separate APPROVED envelope and receipt integrated; repository resolver `AVAILABLE` for authorized validated input;
- assignment schema/validator/test suite: exact publication head 65/65 PASS; post-merge 7/7 SUCCESS;
- S08: `UNAVAILABLE`;
- S09: `UNAVAILABLE_CURRENT`;
- Site Authority schema/validator/promotion suite: 59/59 PASS and post-merge verified;
- adapter/runtime: not implemented.

## 3. Accepted Site Authority transition

The original DRAFT remains immutable historical evidence. The matching APPROVED envelope and approval receipt remain in the protected registry outside `docs/`; public material contains no exact site fact, protected digest or internal locator. ARB disposition was `APPROVED WITH CONDITIONS — 98/100`; Release Quality was `CONDITIONALLY READY`, with no waiver, Blocker or Major.

## 4. Resume criteria

Review and integrate the F3-A3 partial owner decision and repository-only Google Cloud package. Then close F3-OD05 with one exact JPL SPK, coverage, provenance, notices and SHA-256. Do not run GCP bootstrap/apply, acquire artifacts, execute the spike or modify runtime.

## 5. Boundary and rollback

No public exact coordinates, elevation, exact address, protected locator or digest; no runtime provider, forecast, ranking, readiness, go/no-go, EAGLE operation or Safety Authority change. PR #204 rolls back by reviewed Git revert of the APPROVED envelope and receipt while retaining historical DRAFT evidence. `ARB-204-MI01` and `ARB-204-MI02` remain tracked preconditions for future revisions and adapters.

## D4 acceptance update — 15/09/2026

PR #207 passed 5/5 exact-head workflows at `d845042c1e2094cd82de762e7e2de60e9c54b2c5`, was reviewed without Blocker or Major, merged with expected-head control and completed 7/7 post-merge workflows. The DRAFT is not approved and cannot resolve current setup; no receipt or runtime adapter exists. The process is stopped at the mandatory human exact-digest approval gate.


## D5 approval/promotion update — 15/09/2026

The exact-digest human gate is satisfied. PR #209 contains the separate protected receipt and an `APPROVED` lifecycle envelope with unchanged payload/digest, plus closed-schema and fail-closed promotion checks. The dedicated implementation workflow is SUCCESS with 65/65 tests. S09 remains `UNAVAILABLE_CURRENT` because no runtime adapter exists.

## D5 acceptance reconciliation — 15/09/2026

F3-A2-D5 is ACCEPTED / POST-MERGE VERIFIED. Repository authority is `APPROVED/AVAILABLE` only for authorized validated input. S08 remains `UNAVAILABLE`; runtime S09 remains `UNAVAILABLE_CURRENT` because no adapter exists. No EAGLE activity, device command, readiness/go-no-go or Safety Authority change occurred. The next action is dependency-ready successor selection, not runtime activation.


## F3-A3 program handoff — 15/09/2026

PR #210 merged the D5 acceptance reconciliation as `a1d2d84f9f787c247516bc825cb83d2bc15a43f5` and passed 7/7 post-merge workflows. The Program Architect selected F3-A3 because F3-A1/A2 repository authorities are complete and F3-B depends on an accepted method decision. The current handoff is documentation-only; no provider/library/kernel is selected and S10 remains `UNAVAILABLE`.


## F3-A3 decision-preparation — 15/09/2026

PR #211 integrated the Program Assessment/Handoff as `4f76f6646769df378859fbb15147851b4d0543fe` and passed 9/9 post-merge workflows. BKL-031-F3-A3-SOLUTION-001, proposed ADR-010 and BKL-031-F3-A3-VAL-001 now form the current review package. Official sources were refreshed, but observed versions are not approved pins. The conditional recommendation favors local execution and a restricted reference path; F3-OD04–F3-OD10 remain owner decisions, all spike cases remain NOT EXECUTED and S10 remains `UNAVAILABLE`.

## F3-A3 partial decision and cloud preparation — 15/09/2026

The owner approved Astropy primary, Skyfield cross-check, Horizons validation-only, the per-metric accuracy budget, IERS policy, synthetic-site privacy rule, Cloud Run Job profile and capacity envelope. F3-OD05 remains open. The repository now prepares Terraform for WIF, private buckets, a no-NAT VPC and a digest-pinned Cloud Run Job, plus validation-only CI. No native Google Cloud ChatGPT connector was available; after one-time administrator bootstrap, GitHub OIDC/WIF is the keyless automation path. Apply, artifacts and execution remain `NOT EXECUTED`; S10 remains `UNAVAILABLE`.
