name: BKL-031 F7 One Fresh Forecast Validation Request
on:
  push:
    branches: [feat/bkl-031-f7-fresh-forecast-runtime-boundary]
    paths:
      - governance/forecast-evidence/BKL031-F7-PROVIDER-REQUEST-AUTH-001.json
permissions:
  contents: read
concurrency:
  group: bkl-031-f7-one-fresh-forecast-validation-request
  cancel-in-progress: false
jobs:
  acquire-once:
    if: ${{ github.run_attempt == 1 }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 2
      - name: Verify authorization was added in this push
        env:
          BEFORE: ${{ github.event.before }}
          AFTER: ${{ github.sha }}
        run: |
          test "$GITHUB_SHA" = "$AFTER"
          git diff --diff-filter=A --name-only "$BEFORE" "$AFTER" | grep -qx 'governance/forecast-evidence/BKL031-F7-PROVIDER-REQUEST-AUTH-001.json'
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Preflight owner authorization and exact request plan
        run: node .github/scripts/observation-planner-forecast-f7-acquire.mjs --preflight
      - name: Execute exactly one F7 validation request
        id: acquire
        run: node .github/scripts/observation-planner-forecast-f7-acquire.mjs --execute --confirm=F7_ONE_VALIDATION_REQUEST --out=work/f7-evidence
      - uses: actions/upload-artifact@v4
        if: always() && steps.acquire.outcome != 'skipped'
        with:
          name: bkl-031-f7-evidence-${{ github.run_id }}
          path: work/f7-evidence
          if-no-files-found: error
          retention-days: 7
